import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
  container: {
    maxWidth: "100%",
    margin: 30,
  },
  
  headerContainer: {
    marginBottom: 15,
    marginTop: 15
  },
});

export default styles;
